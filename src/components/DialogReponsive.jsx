import * as React from 'react';
import Dialog from "@mui/material/Dialog";
export default function DialogReponsive({ open, setOpen, children }) {

    const handleClose = () => {
        setOpen(false);
    };

    return (
        <React.Fragment>

        </React.Fragment >
    );
}